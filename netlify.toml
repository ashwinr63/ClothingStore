[build]
  command = "bun install && bun run build"
  publish = "build"

[build.environment]
  BUN_VERSION = "1.3.13"
  NETLIFY_SKIP_INSTALL = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
